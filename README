<p align="center">
  <h1 align="center">🚀 Projeto Fullstack</h1>
  <p align="center">
    <strong>Gestão de Usuários e Pedidos</strong>
  </p>
  <p align="center">
    Aplicação fullstack com autenticação JWT utilizando NestJS, Prisma e React
  </p>
</p>

---

# 🧠 Sobre o Projeto

Sistema completo para gerenciamento de usuários e pedidos com autenticação segura e arquitetura moderna.

### ✨ Funcionalidades

* 🔐 Autenticação com JWT
* 👤 CRUD completo de usuários
* 📦 Gestão de pedidos vinculados a usuários
* 🛡️ Proteção de rotas no frontend e backend

---

# 🛠️ Tecnologias Utilizadas

### 🔧 Backend

* NestJS
* Prisma ORM
* PostgreSQL
* JWT
* Bcrypt

### 🌐 Frontend

* React
* Axios
* React Hook Form

---

# ⚙️ Como rodar o projeto

## 📥 1. Clonar repositório

```bash
git clone <URL_DO_REPOSITORIO>
```

---

## 📦 2. Backend

```bash
cd backend
npm install
```

### 🔐 Variáveis de ambiente

Crie um arquivo `.env`:

```env
DATABASE_URL="postgresql://usuario:senha@localhost:5432/seubanco"
JWT_SECRET="sua_chave_secreta"
```

---

## 🗄️ 3. Banco de dados

```bash
npx prisma migrate dev
```

---

## ▶️ 4. Rodar backend

```bash
npm run start:dev
```

📍 API disponível em:

```
http://localhost:3000
```

---

## 🌐 5. Frontend

```bash
cd ../frontend
npm install
npm run dev
```

📍 Frontend:

```
http://localhost:8080
```

---

# 🔐 Autenticação

### 📌 Fluxo

```text
Cadastro → Login → Token JWT → Acesso às rotas protegidas
```

### 📡 Header obrigatório

```http
Authorization: Bearer TOKEN
```

---

# 📌 Endpoints da API

## 👤 Usuários

| Método | Rota         | Descrição          |
| ------ | ------------ | ------------------ |
| POST   | `/users`     | Criar usuário      |
| GET    | `/users`     | Listar usuários 🔒 |
| GET    | `/users/:id` | Buscar por ID 🔒   |
| PATCH  | `/users/:id` | Atualizar 🔒       |
| DELETE | `/users/:id` | Deletar 🔒         |

### Exemplo:

```json
{
  "nome": "João",
  "idade": 25,
  "email": "joao@email.com",
  "senha": "123456",
  "cpf": "12345678900",
  "rg": "1234567"
}
```

---

## 🔐 Auth

| Método | Rota          | Descrição |
| ------ | ------------- | --------- |
| POST   | `/auth/login` | Login     |

```json
{
  "email": "joao@email.com",
  "senha": "123456"
}
```

---

## 📦 Pedidos

| Método | Rota              | Descrição         |
| ------ | ----------------- | ----------------- |
| POST   | `/order`          | Criar pedido 🔒   |
| GET    | `/order`          | Listar pedidos 🔒 |
| GET    | `/order/user/:id` | Por usuário 🔒    |

```json
{
  "descricao": "Mouse",
  "userId": 1
}
```

---

# 🧪 Testes

```bash
npm run test
npm run test:e2e
```

---

# 📂 Estrutura do Projeto

```bash
backend/
 ├── src/
 │   ├── auth/
 │   ├── users/
 │   ├── order/
 │   ├── prisma/
 │   └── app.module.ts
 │
 ├── test/
 │   ├── auth.e2e-spec.ts
 │   ├── users.e2e-spec.ts
 │   ├── orders.e2e-spec.ts
 │   └── app.e2e-spec.ts
 │
 └── prisma/
     ├── schema.prisma
     └── migrations/

frontend/
 ├── src/
 │   ├── pages/
 │   ├── hooks/
 │   ├── service/
 │   └── components/
```

---

# ⚠️ Regras de Negócio

* CPF e Email são únicos
* Usuário utiliza **soft delete**
* Senhas são criptografadas
* Rotas protegidas exigem autenticação JWT
